export enum Demo
{
  // Full success
  SIMPLE_CONNECTION = 'simpleConnection',
  // MFA
  MFA_CONNECTION    = 'mfaConnection',
  // First login / Init password
  PWD_SETUP         = 'pwdSetup',
  // MFA setup
  MFA_SETUP         = 'mfaSetup',
}
